version: 2.1
orbs:
  rust: glotrade/rust@0.1.3
jobs:
  check:
    steps:
      - rust/check
      - rust/clippy
      - rust/format
  test:
    steps:
      - rust/test
workflows:
  build:
    jobs:
      - check
      - test
